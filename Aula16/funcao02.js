function soma (n1=0, n2=0) { // atribuindo o valor de 0 aos parametros formais impede que de erro caso não seja 
    return n1 + n2           // atribuido o paramentro real    
}

console.log (soma(2))